# List backups for a file

List backups for a file

## Usage

``` r
list_backups(file, root = project_root())
```

## Arguments

- file:

  File path relative to project root

- root:

  Project root path. Defaults to current RStudio project.

## Value

Character vector of backup filenames
